let frutas = ['manzanas','peras','lulo'];   // creamos el arreglo

let mas_frutas = frutas.push('fresas');    // agregamos mas frutas al final del arreglo

let borrar = frutas.pop();                 // elimina el ultino valor del arreglo

let add_init = frutas.unshift('mango');    // agrega un valor al pricipio del arreglo

let delete_init = frutas.shift();          // elimina el primer elemento del arreglo

let index1 = frutas.indexOf('peras');      // me indica la posicion del elemento llamado


